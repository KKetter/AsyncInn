@model AsyncInn.Models.RoomAmenities

@{
    ViewData["Title"] = "Edit";
    Layout = "_Layout";
}

<h1>Edit</h1>

<h4>RoomAmenities</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        
        <form asp-action="Edit" asp-controller="RoomAmenities">
            @*@<div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="AmenitiesID" />
        <input type="hidden" asp-for="RoomID" />
        <div class="form-group">
        <table>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Amenities.ID)
                    @
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Room.ID)
                </td>
            </tr>
        </table>
        </div>
        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
        </div>*@
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AmenitiesID" class="control-label"></label>
                <select asp-for="AmenitiesID" class="form-control" asp-items="ViewBag.AmenitiesID"></select>
            </div>
            <div class="form-group">
                <label asp-for="RoomID" class="control-label"></label>
                <select asp-for="RoomID" class="form-control" asp-items="ViewBag.RoomID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
